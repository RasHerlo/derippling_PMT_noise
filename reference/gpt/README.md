# GPT adaptive fringe filter (reference)

Copied from:

`F:\bPACNewData2026\260511\C1_RLV_LW_maybe\LED_x15_Level3b\DATA\SUPPORT_ChanB\to build FFT deripple\from GPT`

- `pmt_fringe_adaptive.py` — earlier adaptive narrow-peak attenuation
- `adaptive_compare.png` — example before / cleaned / removed
- `pmt_fringe_raw_adaptive.py` — newer raw-only ridge/family tracker with gating

This folder is **reference**. Production choice still pending bake-off results in `cursor tests`.
